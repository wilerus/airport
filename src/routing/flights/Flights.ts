import Flights from '../../modules/flights/Flights.vue';

const flightsRoutes = [{ name: 'Flights', path: '/Flights', component: Flights }, { name: 'Default', path: '/', component: Flights }];

export default flightsRoutes;
